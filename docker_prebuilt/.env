# When running via Docker Compose, your browser opens frontend on localhost:5173
# and the backend is exposed on localhost:5000
VITE_API_BASE_URL=http://localhost:5000
COMPOSE_PROJECT_NAME=bps_sensitivity_analysis_tool_final
